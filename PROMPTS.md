# PROMPTS

## 의존성 설치와 개발 환경 설정

### 프롬프트 1

```
tailwindcss v4, react-query, msw, vitest, react-virtual 등 필요한 의존성 패키지 항목에 맞게 설치해
```

### AI 출력 요지

의존성 설치와 Vite·Vitest·Tailwind·ESLint 연결. 판단 두 가지가 들어갔다.

- `test` 스크립트를 `vitest run`으로 고정하고 워치는 `test:watch`로 분리했다.
워치 모드면 커밋 게이트가 응답을 기다리며 멈춘다.
- 단계별 색과 `:focus-visible` 링을 `@theme` 한 곳에 정의했다. 링을 유틸리티로 흩으면
키보드 커밋에서 빠뜨리는 곳이 생긴다.

### 리뷰 / 검증

- 무엇이 문제였나
  - pnpm 11이 MSW 빌드 스크립트를 차단해 `lint`·`test`·`build`가 전부 실행조차 되지 않았다
  - `@vitest/coverage-v8`을 근거 없이 설치해 두고 커버리지 설정도 스크립트도 만들지 않았다
- 어떻게 알아챘나
  - `pnpm lint`를 돌렸더니 린트 결과가 아니라 `ERR_PNPM_IGNORED_BUILDS`가 나왔다
  - `package.json`의 `pnpm.onlyBuiltDependencies`를 넣었더니 "no longer read by pnpm" 경고만 떴고,
  pnpm이 다시 쓴 `pnpm-workspace.yaml`에서 `allowBuilds` 키를 찾았다
  - 미사용 의존성은 code-reviewer가 잡았다
- 채택 / 수정 / 기각
  - 수정. `pnpm-workspace.yaml`의 `allowBuilds`로 풀었다
  - 채택. `@vitest/coverage-v8`을 제거했다.
- 어떻게 고쳤나
  - 설치 명령이 아니라 워크스페이스 설정이 문제였다. 버전이 바뀌며 설정 위치가 옮겨간 경우라
  같은 키를 다른 파일에서 찾는 것으로 해결했다

---

## [mock-api] 지연·실패를 시뮬레이션하는 mock API

### 프롬프트 1

```
정책서 보고 mock-api 구현 진행하자
```

### AI 출력 요지

MSW 핸들러 3개와 localStorage 저장소. 설계 판단 세 가지가 들어갔다.

- 조회 실패율과 이동 실패율을 분리했다. 값이 하나면 목록이 안 떠서 이동 실패 롤백을 시연할 수 없다.
- 실패 주사위를 검증 통과 후 저장 직전에 굴렸다. 순서가 반대면 "실패했는데 데이터는 바뀐" 상태가 생긴다.
- `browser.ts`와 `server.ts`가 같은 `handlers`를 쓴다. mock을 두 벌 만들면 한쪽만 고쳤을 때 테스트가 통과해 버린다.

### 리뷰 / 검증

- 무엇이 문제였나
  - `test/setup.ts`가 매 테스트 후 실패율을 15%로 되살려, 이후 모든 테스트가 15% 확률로 깨질 상황이었다
  - 테스트가 0개인데 `--passWithNoTests`라 커밋 게이트에 초록불이 떴다
- 어떻게 알아챘나
  - 15% 실패는 화면으로 재현이 안 된다고 판단했다. 눈으로는 "가끔 이상한데" 수준이라
  code-reviewer에 재현 조건을 지정하고 추측성 지적을 금지해 돌렸다
  - 지연과 실패 동작은 실제로 실행해 확인했다. 지연 표본 min 200.8 / max 797.8ms,
  `moveFailRate:1` PATCH 시 500 응답 후 저장소 불변
- 채택 / 수정 / 기각
  - 수정. 테스트 기준선을 지연 0·실패율 0으로 잡았다. mock 기본값은 건드리지 않아
  "실패율을 낮추지 않는다"와 충돌하지 않는다
  - 채택. 도달 불가인 `typeof location` 분기를 삭제했다
  - 보류. 스키마 버전과 응답의 순서 판별 수단은 `race-condition`에서 `version`을 추가할 때 함께 본다.
  지금 넣으면 쓰는 데 없는 방어 코드가 된다
- 어떻게 고쳤나
  - `setup.ts`의 `afterEach(resetConfig)`를 `beforeEach` 기준선으로 옮겼다.
  되돌리는 대신 매 테스트 시작을 고정하는 쪽이다

---

## [mock-api] 계약 테스트와 캐시 버그 수정

### 프롬프트 1

```
code-reviewer 지적 중 캐시 버그부터 테스트로 재현하자
```

### AI 출력 요지

계약 테스트 17개. 화면 없이 Vitest + MSW `server`로 전부 검증된다.
앞 커밋에서 `handlers`를 공유하게 해둔 덕에 별도 mock 없이 그대로 붙였다.

### 리뷰 / 검증

- 무엇이 문제였나
  - `db.ts`의 모듈 캐시가 localStorage 초기화를 무시했다. 저장소를 비워도 옛 값이 나오고
  다음 쓰기가 옛 데이터를 되살린다
- 어떻게 알아챘나
  - 지적을 그대로 옮기지 않고 `db.test.ts`로 재현했다. 첫 테스트가 틀렸다 -
  시드의 `c-0001`이 원래 `hired`라 `hired`로 옮기는 게 무변화 이동이었다
  - 다른 단계로 옮기도록 고친 뒤, 수정 전 코드로 되돌려 실패하는 것을 확인하고 나서 고쳤다
- 채택 / 수정 / 기각
  - 수정. 캐시 무효화 시점을 관리하는 대신 캐시를 없앴다. 동기화할 게 없으면 어긋날 수도 없다
- 어떻게 고쳤나
  - `load()`가 매번 localStorage를 읽는다. 부수적으로 내부 배열 참조 유출도 사라졌다

## [services] 지원자 API 서비스·쿼리 레이어

### 프롬프트 1

```
mock-api 기반 services, queries 구조 작업해
```

### AI 출력 요지

앞 커밋의 mock 라우트 3개 위에 `services/{instance,interceptors,api}` + `queries/candidate/{queries,mutations}` + `providers`.
참고한 두 레포가 갈리는 지점이 있었다. 한쪽은 응답을 `APIResponse<T>`로 감싸 쿼리에서 벗기고,
다른 쪽은 서비스가 벗겨 도메인 타입을 반환한다. 우리 mock은 envelope 없이 raw를 주므로 후자를 골랐다.

### 리뷰 / 검증

- 무엇이 문제였나
  - 패턴을 따라간다는 게 파일을 복사해 오는 것으로 흘렀다. 참고 레포의 `todo.ts`가 도메인만 다른 채
  그대로 들어와 있었고, 그것 때문에 빌드가 깨진 상태로 작업이 진행되고 있었다
- 어떻게 알아챘나
  - 구조를 다 세운 뒤 `pnpm build`를 돌려서. 처음부터 돌렸으면 더 일찍 잡았을 것이다
- 채택 / 수정 / 기각
  - 수정. 잔재를 지우고 우리 도메인으로 다시 썼다
  - 기각. envelope 패턴. 그대로 따르려면 이미 테스트까지 붙은 mock을 고쳐야 했다.
  참고 대상이 둘일 때 무엇을 따르고 무엇을 버릴지가 이 작업의 실제 판단이었다
- 어떻게 고쳤나
  - 서비스가 `res.data`만 벗겨 도메인 타입을 반환하도록 통일

### 프롬프트 2

```
상세 조회랑 목록 재조회 지적 재현해보고 고치자
```

### AI 출력 요지

P1 3건을 probe 테스트로 재현한 뒤 수정.

### 리뷰 / 검증

- 무엇이 문제였나
  - 상세 조회에 빈 id가 들어가면 목록 응답이 상세 자리에 들어온다
  - 이동에 성공해도 뒤따르는 목록 재조회가 15%로 실패해 화면이 에러가 된다
- 어떻게 알아챘나
  - 지적을 그대로 옮기지 않고 재현했다. 앞선 리뷰에서 오탐이 한 번 있었다
  - 목록 에러는 첫 재현이 성공으로 나와 오탐인 줄 알았다. `invalidateQueries`가 활성 옵저버가
  붙은 쿼리만 재조회한다는 걸 내가 몰랐던 것이고, 옵저버를 붙이자 재현됐다.
  재현이 안 됐다고 지적을 버렸으면 그대로 커밋할 뻔했다
- 채택 / 수정 / 기각
  - 수정. 이동 뒤 목록을 다시 받지 않기로 했다. 실패할 수 있는 요청을 하나 줄이는 쪽이,
  실패를 잘 처리하는 것보다 낫다고 봤다
- 어떻게 고쳤나
  - 빈 id를 상세 조회 진입 전에 막고, 이동 응답을 캐시에 직접 반영해 재조회를 없앴다

---

---

## [design-tokens] 화면 작업에 쓸 디자인 토큰을 @theme 에 추가

### 프롬프트 1

```
정책에서 맞게 대시보드 디자인 테일윈드 토큰 생성 진행해
```

### AI 출력 요지

받아둔 자료의 토큰을 `@theme` 으로 옮겼다 — 단계 전경 5·배지 배경 5·상태 4,
애니메이션 2(`--animate-shake`/`--animate-shimmer`), 한글 폰트 스택·`keep-all`·reduced-motion 가드.

### 리뷰 / 검증

- 무엇이 문제였나
  - Tailwind v4 는 안 쓰는 테마 변수를 트리셰이킹한다. `var(--color-blue-500)` 참조가 빌드 후
  빈 값으로 남을 수 있다는 의심이 있었고 문서만으로는 확답이 안 나왔다
  - 토큰이 지운 스캐폴드 변수(`--accent`/`--text-h`/`--border`)를 `App.css` 가 참조해
  Vite 시작 페이지가 깨졌다
  - 초안이 받은 자료를 "확정안"으로 읽어, 이번에 내리지도 않은 색 선택 근거를 기록에 썼다
- 어떻게 알아챘나
  - 임시 프로브에 새 유틸리티를 나열해 `vite build` 후 `dist/assets/index-*.css` 를 grep.
  팔레트 변수 15개가 전부 `:root` 에 oklch 값으로 나왔고 유틸리티·`@keyframes` 도 생성됐다
  - 에이전트가 센 14/16 은 `@theme` 블록만 세어 `body` 의 `slate-50`·`slate-900` 을 빠뜨린 값이다.
  빌드 산출물과 distinct 참조 수가 둘 다 15로 일치한다
- 채택 / 수정 / 기각
  - 기각. mockup 의 `:focus-visible { border-radius: 4px }` — 모든 포커스 요소에 걸려
  알약 모양 필터 칩 모서리가 각진다
  - 기각. mockup 의 `--radius`·`shadow`·`skeleton` 변수 — Tailwind 기본 유틸리티로 충분하다
  - 수정. 색 선택 근거를 기록에서 걷어냈다. 옮겨 적은 것뿐이고 이번에 내린 설계 결정은 없다
- 어떻게 고쳤나
  - `index.css` 안의 스캐폴드 규칙(`#root` 고정폭·`h1`·다크모드)과 `App.css`·데모 이미지
  3개·`public/icons.svg` 를 지우고 `App.tsx` 를 최소 placeholder 로 바꿨다
  - 게이트 — `tsc -b` 0, `eslint .` 0, `vite build` 성공, 테스트 17/17

### 프롬프트 2

```
PROMPTS.md에서 커밋 해시 빼자. 해시를 넣으니 커밋 로그가 2개로 늘어나
```

### AI 출력 요지

전 섹션의 해시 줄을 지웠다. scope 와 섹션 제목이 이미 1:1이라 해시는 중복이다.

### 리뷰 / 검증

- 무엇이 문제였나
  - 해시를 적으려면 커밋이 먼저 있어야 하고, 커밋에 섹션을 담으려면 섹션이 먼저 있어야 한다.
  이 순환 때문에 기록이 매번 별도 `docs:` 커밋으로 빠져 기능당 커밋이 2개가 됐다
- 어떻게 알아챘나
  - `git log -- PROMPTS.md` 로 확인했다. 기존 4개 섹션 전부 기능 커밋이 아니라
  나중 `docs:` 커밋에서 들어갔다. CLAUDE.md 의 "기능 커밋에 함께 스테이지한다"가 4번 다 안 지켜졌다
- 채택 / 수정 / 기각
  - 채택. 해시를 뺀다. CLAUDE.md 가 이미 "커밋 scope와 섹션 제목이 일치한다"로 연결을 정해뒀고,
  찾을 일이 있으면 `git log --grep` 이면 된다
- 어떻게 고쳤나
  - 기존 4개 섹션의 해시 줄까지 함께 지웠다. 이제 기록이 기능 커밋에 같이 들어간다

---

## [board-layout] 5단계 컬럼 보드

### 프롬프트 1

```
board-layout 작업 진행하자
```

### AI 출력 요지

`STAGES` 순서로 컬럼 5개, 헤더에 단계 색 점과 건수, 가로 스크롤.
카드는 다음 커밋 몫이라 본문에는 이름만 한 줄씩 뿌렸다. 그래야 그룹핑과 스크롤이 눈으로 검증된다.

- Tailwind 는 소스를 정적 스캔한다. `bg-stage-${stage}` 는 클래스가 생성되지 않아
`STAGE_DOT_CLASS` 리터럴 매핑을 상수로 뺐다.
- 각 항목에 `data-index` 를 미리 심었다. 키보드 포커싱과 가상 스크롤이 같은 속성을 쓴다.

### 리뷰 / 검증

- 무엇이 문제였나
  - 컴포넌트를 평평한 파일로 뒀다. 전용 훅과 하위 컴포넌트를 둘 자리가 없다
  - 컬럼 제목이 `aria-label` 뿐이라 헤딩 탐색으로 컬럼에 도달할 수 없고,
  `<ul>` 에 `role="list"` 가 없어 Safari VoiceOver 에서 목록 시맨틱이 사라진다
  - 건수 pill 이 "412" 만 낭독된다
- 어떻게 알아챘나
  - 브라우저에서 직접 확인했다. 컬럼 순서, 점 색 5종, 건수 합 1,000 을 눈으로 셌다
  - 접근성은 a11y-checker 에 "뒤 커밋에서 마크업을 뒤집지 않아도 되는가" 를 기준으로 주고 돌렸다
- 채택 / 수정 / 기각
  - 수정. `<Name>/index.tsx` + `use<Name>.ts` + `components/` 구조로 바꿨다.
  중간에 지시를 잘못 읽어 `views/` 를 새로 만들었다가 되돌렸다 - 참고하라는 건 디렉터리가 아니라 작성 방식이었다
  - 채택. 접근성 3건은 지금 고쳤다. 카드나 roving tabindex 와 무관해 뒤 커밋에서 되돌릴 일이 없다
  - 기각. 가로 스크롤 컨테이너에 `tabIndex={0}` 을 넣지 않았다. 지금 넣으면
  카드가 포커스 가능해지는 다음 커밋에서 원치 않는 탭 스톱을 제거해야 한다
- 어떻게 고쳤나
  - 쿼리와 그룹핑을 `useBoard` 로 빼고 `index.tsx` 는 렌더만 하게 했다
  - `aria-labelledby` + `<h2>`, `role="list"`, 건수에 `sr-only` 단위, 색 점에 `aria-hidden`

### 프롬프트 2

```
그룹핑 테스트가 진짜 잡는지 확인하고 반영하자
```

### AI 출력 요지

P1 3건 중 2건을 고쳤다. 나머지 하나는 쓰는 커밋에서 잡기로 했다.

### 리뷰 / 검증

- 무엇이 문제였나
  - **테스트가 거짓 초록이었다.** 컬럼별 합계만 봐서 1,000명이 한 컬럼에 몰려도 통과한다
  - 보드 높이 `8rem` 이 실제 헤더 크롬 92px 과 안 맞아 36px 을 버린다.
  `Board` 가 `App` 의 레이아웃 수치를 하드코딩한 탓이다
- 어떻게 알아챘나
  - 리뷰어가 그룹핑을 일부러 깨뜨려 테스트를 돌렸더니 그대로 통과했다.
  지적을 받고 나서 같은 방식으로 직접 재현했다
- 채택 / 수정 / 기각
  - 수정. 컬럼별 인원수와 첫 지원자 이름을 시드와 대조하도록 테스트를 다시 썼다
  - 수정. 높이를 `flex-1 min-h-0` 으로 바꿔 숫자 자체를 없앴다
  - 보류. 테스트 클라이언트의 `retry: 1` 이 에러 노출을 1037ms 로 늘려
  `findBy*` 기본 타임아웃을 넘긴다. 에러 UI 를 실제로 테스트하는 커밋에서 잡는다
- 어떻게 고쳤나
  - 고친 뒤 다시 그룹핑을 깨뜨려 **테스트가 실패하는 것까지 확인**했다.
  통과만 보고 끝냈으면 거짓 초록을 그대로 뒀을 것이다

---

## [card-list] 지원자 카드

### 프롬프트 1

```
card-list 작업 진행해
```

### AI 출력 요지

`CandidateCard` 를 만들어 앞 커밋의 `<li>` 안쪽만 교체했다. 이름, 직무, 단계 배지, 지원일 4종.

- 배지는 `STAGE_BADGE_CLASS` 로 따로 뺐다. 점은 `bg-*` 하나지만 배지는 `bg-*-soft text-*` 두 개다.
역할이 달라 앞 커밋의 점 매핑과 합치지 않았다.
- 날짜는 `<time dateTime>` 으로 감쌌다. 화면에는 `2026-05-01` 이 보이지만 기계가 읽을 값은 ISO 원본이다.

### 리뷰 / 검증

- 무엇이 문제였나
  - 날짜 포맷을 카드 안에서 문자열로 자르면 다른 화면에서 또 자르게 된다
- 어떻게 알아챘나
  - 브라우저에서 확인했다. 배지 색이 컬럼 헤더 점과 같은 토큰을 보는지, 5개 단계가 각각
  다른 색으로 나오는지 눈으로 대조했다
  - 4종 노출은 테스트로 고정했다. 시드에서 지원자를 하나 꺼내 이름, 직무, 단계 라벨,
  `datetime` 속성을 각각 단언한다
- 채택 / 수정 / 기각
  - 수정. 포맷터를 `lib/format.ts` 로 뺐다. `sv-SE` 로케일이 `YYYY-MM-DD` 를 그대로 준다
  - 기각. 카드에 이동 버튼을 넣지 않았다. 다음 커밋 몫이라 지금 넣으면 두 기능이 한 커밋에 섞인다
- 어떻게 고쳤나
  - `BoardColumn` 의 `<li data-index>` 래퍼는 그대로 두고 안쪽만 바꿨다.
  키보드와 가상 스크롤이 쓸 속성이 살아 있다

### 프롬프트 2

```
배지 대비랑 잘린 텍스트 지적 확인하고 고치자
```

### AI 출력 요지

P1 3건 수정. 나머지는 각각 쓰는 커밋이 따로 있어 미뤘다.

### 리뷰 / 검증

- 무엇이 문제였나
  - 배지 5종이 전부 대비 기준 미달이었다. `-soft` 배경에 500 전경을 얹어 최악이 2.07:1,
  11px 본문이라 4.5:1 이 기준이다
  - 지원일만 `slate-400`(2.6:1). 바로 위 직무 줄은 `slate-500`(4.76:1) 이라 이 줄만 예외였다
  - `<li>` 의 `truncate` 를 지우고 카드로 바꾸면서 오버플로 가드가 사라졌다.
  전역 `word-break: keep-all` 탓에 긴 한글 직무명이 카드 밖으로 넘친다
- 어떻게 알아챘나
  - 리뷰가 13건을 한 번에 쏟아내 읽기 어려웠다. 분량 규칙을 지키게 해 다시 받으니
  P1 3건으로 정리됐고 내가 먼저 추린 것과 같았다
  - 대비 수치는 리뷰가 계산해 줬다. 직접 잰 것이 아니라 토큰 짝이 500/50 인 것을 코드에서 확인했다
- 채택 / 수정 / 기각
  - 수정. 배지 전경용 `-strong` 토큰을 따로 뒀다. 500 은 컬럼 헤더 점이 계속 쓴다.
  점은 텍스트가 아니라 대비 기준이 다르므로 한 토큰으로 합치지 않았다
  - 보류. `formatDate` 테스트, 배지 클래스 검증, hover 동작, `<article>` 중첩, 6배 DOM 은
  각각 쓰는 커밋이 따로 있다. 지금 하면 이 커밋이 남의 몫을 가져온다
- 어떻게 고쳤나
  - 오버플로는 내가 만든 회귀였다. 카드로 교체하면서 `<li>` 가 하던 일을 옮기지 않았다

---

## [stage-move] 단계 이동

### 프롬프트 1

```
stage-move 진행하자
```

### AI 출력 요지

카드에 이동 버튼을 붙이고 `PATCH` 로 저장한다. 낙관적 반영은 다음 커밋 몫이라
지금은 응답을 받고 나서 화면이 바뀐다.

- 드래그앤드롭 대신 액션 버튼을 골랐다. 키보드 대체 경로를 따로 만들 필요가 없다.
- 이동 로직을 `useCandidateCard` 로 빼서 카드는 렌더만 한다.

### 리뷰 / 검증

- 무엇이 문제였나
  - 단계 이동 규칙이 요구사항에 없다. 순서를 강제할지 임의 이동을 허용할지 정해야 했다
- 어떻게 알아챘나
  - 테스트를 쓰다 시드의 `c-0001` 이 `hired` 라 면접 버튼이 없어 실패했다.
  단계마다 버튼 구성이 다르다는 걸 그때 확인했다
- 채택 / 수정 / 기각
  - 기각. 확인 모달을 넣지 않았다. 요구되지 않은 규칙을 발명하면 범위만 넓어진다
- 어떻게 고쳤나
  - 테스트가 특정 id 를 찍지 않고 `screening` 지원자를 찾아 쓰도록 바꿨다
  - 버튼에 `aria-label` 로 "누구를 어디로" 를 넣었다. "면접 →" 만으로는 어느 카드의 버튼인지 모른다

### 프롬프트 2

```
최종합격 다음이 불합격으로 나오는 것부터 고치자
```

### AI 출력 요지

파이프라인을 `STAGES` 배열에서 떼어냈다. 불합격은 순서상 다음 단계가 아니라 어디서든 빠지는 분기다.

### 리뷰 / 검증

- 무엇이 문제였나
  - `STAGES` 는 컬럼 나열 순서인데 이걸 파이프라인 순서로 썼다.
  최종합격 카드에 `불합격 →` 이 뜨고, 반대로 서류검토나 처우협의에서는 불합격 버튼이 없었다
  - 그래서 탈락시키려면 최종합격을 한 번 거쳐야 했고, 그 중간 상태가 저장소에 기록됐다
- 어떻게 알아챘나
  - 리뷰가 P0 로 잡았다. `adjacentStages('hired')` 가 `next: 'rejected'` 를 주는 걸 코드로 확인했다
  - 경계 처리는 "이상 없음" 판정이었지만, 그 경계가 rejected 인 것 자체가 틀렸다는 지적이었다
- 채택 / 수정 / 기각
  - 수정. 파이프라인 4단계를 별도 상수로 두고 불합격을 분기로 뺐다
  - 채택. 쓰이지 않는 `isMoving` 을 지웠다. 낙관적 업데이트는 응답을 안 기다리는 게 목적이라
  pending 으로 버튼을 잠글 이유가 없고, 연타 방지는 경쟁 상태 커밋 몫이다
- 어떻게 고쳤나
  - 이동 테스트가 거짓 초록이 아닌 것도 함께 확인했다. `mutate` 를 no-op 으로 바꾸니 타임아웃으로 실패했다

### 프롬프트 3

```
추가된 CLAUDE.md 규칙에 맞게 compoents 분리해줘
```

### AI 출력 요지

`BoardColumn` 과 `CandidateCard` 를 각자의 하위 파트로 쪼갰다.

### 리뷰 / 검증

- 무엇이 문제였나
  - `CandidateCard` 가 60줄이었다. 배지, 날짜, 버튼 두 개가 한 파일에 섞여 있었다
  - `BoardColumn` 도 헤더 마크업과 목록을 한 곳에 몰아넣었다
  - `CandidateCard` 를 `Board` 하위에 넣어뒀는데, 상세 패널에서도 쓸 컴포넌트다
- 어떻게 알아챘나
  - 규칙을 먼저 정하고 나서 기존 파일을 다시 보니 어긋난 곳이 드러났다
- 채택 / 수정 / 기각
  - 수정. 형제 컴포넌트는 `components/` 최상위로 올리고, 전용 하위 파트만 `components/` 안에 뒀다
- 어떻게 고쳤나
  - 옮기다 `aria-label` 과 `aria-labelledby` 를 둘 다 붙인 걸 발견해 하나를 뺐다

---

## [optimistic-update] 낙관적 업데이트와 실패 복구

### 프롬프트 1

```
optimistic-update 작업 진행해
```

### AI 출력 요지

서버 사실(쿼리 캐시)은 건드리지 않고 `pending` 오버레이만 둔다.
화면은 `pending[id] ?? candidate.stage` 를 그리고, 성공이든 실패든 오버레이에서 지우기만 한다.

**롤백 코드가 없다.** 원본이 그대로라 지우는 순간 화면이 저절로 복구된다.
목록 전체를 스냅샷 떠서 복원하는 방식이면 A 가 실패할 때 그 사이 성공한 B 까지 되돌아간다.

### 리뷰 / 검증

- 무엇이 문제였나
  - 카드가 "이동 중" 에서 영영 안 풀렸다. API 는 500 을 돌려줬는데 화면이 그대로였다
- 어떻게 알아챘나
  - 브라우저에서 `?moveFailRate=1` 로 실패를 강제하고 눌러봤다. 콘솔에 `[API] 서버 오류` 가
  찍혔는데 카드는 계속 이동 중이었다
  - 원인은 낙관적 반영 그 자체였다. 카드가 다른 컬럼으로 옮겨가면서 React 가 원래 위치의
  컴포넌트를 언마운트하는데, 언마운트된 컴포넌트의 `mutate` 콜백은 호출되지 않는다
- 채택 / 수정 / 기각
  - 수정. 이동을 `Board` 가 소유하게 옮겼다. 카드는 사라져도 보드는 남는다.
  `onMove` 를 세 단계 내려보내는 비용을 치렀지만, 콜백이 안 도는 것보다 낫다
- 어떻게 고쳤나
  - 테스트가 이 경우를 못 잡고 있었다. 지연 0 이라 클릭 직후 이미 실패까지 끝나 있어
  낙관적 반영을 관찰할 틈이 없었다. 지연을 50ms 로 줘 요청을 진행 중으로 유지했다
  - 실패 롤백 테스트가 진짜 잡는지 확인했다. 실패 처리를 비우니 타임아웃으로 실패했다

### 프롬프트 2

```
store 걷어내고 hook 으로 옮기자
```

### AI 출력 요지

ai가 생성한 `src/stores/` 를 통째로 걷어내고 `useBoard` 의 `useState` 두 개로 옮겼다.

### 리뷰 / 검증

- 무엇이 문제였나
  - 오버레이를 모듈 스토어 + `useSyncExternalStore` 로 만들었다. 파일 두 개와 구독 코드가 늘었다
- 어떻게 알아챘나
  - 지적을 받고 다시 보니 이동을 `Board` 로 옮긴 시점에 스토어가 필요 없어졌다.
  `Board` 는 언마운트되지 않으므로 그냥 지역 상태면 된다
- 채택 / 수정 / 기각
  - 채택. 계획서가 경쟁 상태 커밋의 조정자를 React 비의존 모듈로 두라고 했지만,
  그건 그 커밋의 요구지 지금의 요구가 아니다. 미리 만들면 쓰지 않는 코드가 된다
- 어떻게 고쳤나
  - `pending` 을 prop 으로 내려보내게 바꿨다. 카드는 자기 `pendingStage` 하나만 받는다
  - 스토어 테스트 4개가 사라진 대신, 보드 테스트가 화면으로 검증하게 바꿨다.
  실패 후 카드가 원래 컬럼에 있는지를 `data-candidate-id` 로 확인한다

### 프롬프트 3

```
토스트 계속 유지되는데 1초 후 사라지게 작업해줘
```

### AI 출력 요지

`MoveFeedback` 안에서 타이머로 처리했다. 표시 시간은 알림의 관심사지 이동 로직의 관심사가 아니다.

### 리뷰 / 검증

- 무엇이 문제였나
  - 겹쳐서 이동하면 앞 호출의 콜백이 통째로 유실됐다. 카드가 영영 "이동 중" 이고 토스트도 안 떴다
  - 이동 중 카드의 배지만 원래 단계를 가리켰다. 버튼은 보이는 단계 기준인데 배지만 서버값이라
    면접 컬럼의 카드가 "서류검토" 배지를 달았다
- 어떻게 알아챘나
  - 리뷰가 P0 로 잡았다. `useMutation` 을 `useBoard` 에서 한 번만 호출해 옵저버를 모든 카드가
    공유하는데, 두 번째 호출이 첫 mutation 에서 옵저버를 떼어낸다
  - 앞서 고친 언마운트 문제와는 별개 원인이었다. 하나를 고쳤다고 끝난 게 아니었다
- 채택 / 수정 / 기각
  - 수정. `mutate` 를 `mutateAsync` 로 바꿨다. 호출마다 독립된 프로미스라 옵저버 교체와 무관하다
  - 수정. 배지도 보이는 단계를 쓰게 해 카드 안 기준을 하나로 맞췄다
  - 기각. `setState` 를 effect 에서 동기 호출하는 방식. 연쇄 렌더가 나서 lint 가 막는다.
    만료된 대상을 기억하고 비교하는 쪽으로 바꿨다
- 어떻게 고쳤나
  - 두 카드를 겹쳐 옮기는 테스트를 추가했다. 기존 테스트는 전부 단건이라 이 결함을 못 잡았다
