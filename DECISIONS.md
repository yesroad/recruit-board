# 설계 결정

요구사항이 정하지 않은 지점에서 무엇을 왜 골랐는지 남깁니다.

## 1. Vite + MSW, Next.js 기각

- 결정. Vite + MSW
- 후보. Next.js App Router + Route Handlers
- 근거. mock 계약을 브라우저와 Vitest 가 **같은 handler 로 공유**한다. 롤백과 경쟁 상태
  테스트를 실제 화면과 같은 계약 위에서 돌릴 수 있다.
- 버린 이유. Route Handler 로 mock 을 만들면 테스트용 mock 을 한 벌 더 만들게 된다.
  두 벌이 어긋나는 순간 테스트가 거짓말을 한다. SSR 도 이 과제에 필요 없다.

처음에 Next.js 를 골랐다가 조사 후 뒤집었습니다.

## 2. Tailwind v4, CSS Modules 에서 뒤집음

- 결정. Tailwind v4
- 후보. CSS Modules, SCSS Modules, emotion
- 근거. 설정 파일이 0개다. 간격과 색 스케일을 그대로 써서 **네이밍 체계를 설계하는 시간을 안 쓴다.**
- 버린 이유. emotion 은 런타임 스타일 계산이라 성능 측정에 노이즈를 얹는다. CSS Modules 는
  클래스 네이밍과 스케일을 직접 만드는 비용이 그대로 남는다.

**처음엔 CSS Modules 로 정했다가 뒤집었습니다.** 기각 근거로 적었던 "config 와 의존성이 늘어난다"가
Tailwind v3 기준이었고 v4 에서는 사실이 아니었습니다. 판단의 전제가 틀렸으므로 결론을 바꿨습니다.

## 3. 오버레이 상태 모델, 롤백 코드를 없앰

- 결정. 서버 사실(`candidate.stage`)과 낙관적 의도(`pendingStage[id]`)를 분리하고
  화면은 `pendingStage[id] ?? candidate.stage` 를 본다
- 후보. TanStack Query 공식 예제의 목록 스냅샷 후 통째 복원
- 근거. 실패 시 오버레이만 지우면 원본이 그대로라 화면이 저절로 복구된다.
  **롤백 코드 자체가 없으므로 "어느 경로에서 롤백을 빠뜨렸다"는 버그가 구조적으로 불가능하다.**
- 버린 이유. 스냅샷 복원은 A 가 실패할 때 그 사이 성공한 B 까지 되돌린다.

## 4. 카드별 단일 슬롯 + seq, 버튼 disabled 기각

- 결정. 카드마다 `{ inFlight, queued, seq }` 슬롯을 두고 최신 의도 하나만 큐에 유지
- 후보. 요청 중 버튼 `disabled`, 요청 취소(AbortController)
- 근거. 연타 5번이 요청 5번이 되면 15% 실패가 곱해진다. `seq` 로 늦게 온 응답을 폐기한다.
- 버린 이유. `disabled` 는 경쟁 상태를 **없앤 것**이지 처리한 것이 아니다. 사용자 조작을 막는 방식이다.

이 로직은 React 에 의존하지 않는 순수 모듈 `moveCoordinator.ts` 로 뺐습니다.
경쟁 상태는 지연이 랜덤이라 화면으로 재현하기 어렵고, 분리해두면 deferred promise 로
응답 시점을 직접 잡는 테스트가 가능합니다.

## 5. 작업 하네스, 에이전트 6개와 스킬 6개

- 결정. `.claude/` 에 리뷰, 기록, 커밋 전용 에이전트와 스킬을 먼저 구성하고 시작
- 후보. 한 번의 대화로 기능마다 진행
- 근거. 기능이 14개다. 매번 "리뷰해줘, 기록해줘, 커밋 메시지" 를 손으로 지시하면 반드시
  빠뜨린다. 특히 기록은 몰아서 쓰면 그 자체로 신뢰를 잃는다.
- 버린 이유. 대화형은 기능이 늘수록 기준이 흔들린다.

세 가지를 의도적으로 제약했습니다.

1. **검증자에게 수정 권한을 주지 않았습니다.** `code-reviewer` 와 `a11y-checker` 는 `Edit` 이
   없습니다. 에이전트가 바로 고치면 사람이 재현하는 단계를 건너뛰게 되고, 그러면 `PROMPTS.md` 의
   "어떻게 알아챘나" 에 쓸 사실이 남지 않습니다.
2. **리뷰를 커밋 게이트에 넣지 않았습니다.** `lint` 와 `test` 만 넣었습니다. 리뷰 지적은 재현할
   시간이 필요하고, 그 결과가 기록의 재료이기 때문입니다.
3. **기록 스킬이 판단 항목을 인터뷰로 묻습니다.** `prompt-log` 는 사실만 채우고 "왜 그렇게
   판단했나" 는 빈칸으로 두고 물어봅니다. 확인하지 않은 검증을 지어내지 않기 위해서입니다.

---

## AI 제안 중 기각한 것

| 제안 | 기각 사유 |
|---|---|
| shadcn/ui | Dialog 만 값어치가 있는데, **직접 짠 포커스 트랩이 리뷰 기록의 재료다.** AI 초안은 거의 항상 Esc 는 되는데 닫을 때 원래 카드로 포커스 복귀를 빠뜨린다. 실무였다면 반대 결론이다 |
| React Compiler | 병목이 리렌더 횟수가 아니라 1,000건 렌더 대상 수다. 가상 스크롤이 해법이라 빌드 설정 비용만 늘어난다 |
| envelope 응답 패턴 | 이미 테스트까지 붙은 mock 계약을 고쳐야 했다. 얻는 것보다 비용이 크다 |
| 이동 확인 모달 | 요구되지 않은 규칙을 발명하면 범위만 넓어진다 |
| `setState` 를 effect 에서 동기 호출 | 연쇄 렌더가 발생하고 lint 가 막는다 |
| 가로 스크롤 컨테이너 `tabIndex={0}` | 키보드 설계를 먼저 정하고 넣어야 한다. 지금 넣으면 탭스톱만 늘어난다 |

## 세운 가정

요구사항이 모호한 지점은 스스로 정하고 여기 남깁니다.

| 모호한 지점 | 가정 |
|---|---|
| 15% 실패를 읽기에도 적용하나 | 읽기와 쓰기 모두 적용. 그래야 에러 상태를 실제로 시연할 수 있다. 실패율은 따로 조정 가능 |
| 단계 이동 순서 제약 | **제약 없음.** 순서가 요구되지 않았으므로 임의 이동 허용. 규칙을 발명해 범위를 넓히지 않는다 |
| persist 매체 | mock 서버 내부 저장소로 localStorage. 클라이언트는 직접 접근하지 않는다 |
| PROMPTS.md 커밋 위치 | 해당 기능 커밋에 함께 포함. 커밋과 기록이 1:1로 대응해야 한다 |
| 지원자 건수 | 시드 1,000건. 요구된 200건은 이미 포함된다 |
| Undo 이력 깊이 | 1건. "방금 한 이동" 으로 읽었다 |

## 못 끝낸 기능

필수 요구사항 6개는 전부 동작합니다. 가점 항목의 진행 상태입니다.

| 항목 | 상태 |
| --- | --- |
| 웹 접근성 | 완료 |
| 경쟁 상태 처리 | 동작하지만 설계대로 분리하지 못함 |
| 테스트 | 21개 작성. 2건 실패 중 |
| 되돌리기(Undo) | 미착수 |
| 대량 데이터 성능 | 미착수 |

### 경쟁 상태, 동작하지만 설계대로 분리하지 못함

같은 카드를 연속 이동해도 마지막 의도가 서버에 남습니다. 카드마다 슬롯을 두고 진행 중이면
큐에 쌓지 않고 최신 의도로 덮어쓰는 coalescing 이 `useBoard.ts` 안에 있습니다.
느린 첫 응답이 나중에 도착해도 낡은 값이 덮어쓰지 않는 것을 테스트로 확인했습니다.

다만 계획했던 두 가지를 넣지 못했습니다.

- **`seq` 스탬프.** 지금은 coalescing 만으로 순서를 지킵니다. 요청이 세 개 이상 겹치는
  경우까지 확실히 하려면 응답에 순번을 달아 낡은 것을 버려야 합니다.
- **`moveCoordinator.ts` 순수 모듈 분리.** 로직이 훅 안에 남아 있습니다. 분리했다면
  deferred promise 로 응답 시점을 직접 잡는 테스트를 쓸 수 있었습니다. 지금은 MSW 의
  `delay` 로 순서를 만들어 검증하고 있어 제어가 덜 정밀합니다.

동작에는 문제가 없어 우선순위를 뒤로 미뤘습니다.

### 테스트 2건 실패

`loading-error-empty` 에서 상세 패널에 스켈레톤을 넣은 뒤 기존 `detail-panel` 테스트가
깨졌습니다. 패널은 열리는데 내용이 아직 스켈레톤이라 필드를 찾지 못합니다.
로딩 완료를 기다리도록 고쳐야 합니다.

- `카드의 상세 버튼을 누르면 패널이 열리고 상세 필드가 보인다`
- `패널을 열면 포커스가 패널로 간다`

### 되돌리기와 대량 데이터, 미착수

필수 요구사항을 먼저 끝내는 원칙으로 진행했고, 접근성까지 마친 시점에서 남았습니다.

- **Undo.** 오버레이 구조상 직전 이동 한 건을 기억하면 되지만, 되돌리기 요청도 실패할 수
  있고 그 사이 카드가 다시 이동했을 때의 처리가 필요합니다.
- **가상 스크롤.** 1,000건이 모두 DOM 에 올라갑니다. TanStack Virtual 은 설치해 두었습니다.
  컬럼마다 독립 가상화가 필요하고, 도입 후 roving tabindex 의 `Home` 과 `End` 가 가상 영역
  밖의 카드로 갈 때 스크롤 후 포커스를 잡아야 합니다.
